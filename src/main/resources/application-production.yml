spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/nexus_support_ai?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=utf8mb4}
    username: ${DB_USERNAME:nexus}
    password: ${DB_PASSWORD:nexus}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
